{
  "name": "mattis40.github.io",
  "version": "1.0.0",
  "description": "Personal portfolio website",
  "main": "index.js",
  "scripts": {
    "dev": "tailwindcss -i ./src/main.css -o ./src/style.css --watch",
    "build": "tailwindcss -i ./src/main.css -o ./src/style.css && mkdir -p dist && cp -r ./src/* ./dist/ && cp -r ./assets ./dist/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mattis",
  "license": "ISC",
  "devDependencies": {
    "tailwindcss": "^3.4.3"
  }
}